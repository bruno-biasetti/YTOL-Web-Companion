<h1>Editing sharing</h1>

<%= render 'form', custom_url: sharing_path(@sharing, locale: locale) %>

<%= link_to 'Show', sharing_path(@sharing, locale: locale) %> |
<%= link_to 'Back', sharings_path(locale: locale) %>
