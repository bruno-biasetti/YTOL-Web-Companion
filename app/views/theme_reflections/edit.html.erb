<h1>Editing theme_reflection</h1>

<%= render 'form', custom_url: theme_reflection_path(@theme_reflection, locale: locale) %>

<%= link_to 'Show', theme_reflection_path(@theme_reflection, locale: locale) %> |
<%= link_to 'Back', theme_reflections_path(locale: locale) %>
