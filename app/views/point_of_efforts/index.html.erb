<h1>Listing point_of_efforts</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Due date</th>
      <th>Created at</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @point_of_efforts.each do |point_of_effort| %>
      <tr>
        <td><%= point_of_effort.title %></td>
        <td><%= point_of_effort.description %></td>
        <td><%= l point_of_effort.due_date %></td>
        <td><%= l point_of_effort.created_at, format: :date_only %></td>
        <td><%= link_to 'Show', point_of_effort_path(point_of_effort, locale: locale) %></td>
        <td><%= link_to 'Edit', edit_point_of_effort_path(point_of_effort, locale: locale) %></td>
        <td><%= link_to 'Destroy', point_of_effort_path(point_of_effort, locale: locale), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Point of effort', new_point_of_effort_path %>
